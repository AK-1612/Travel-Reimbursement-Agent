# Limitations

- The default demo does not call a hosted LLM.
- Policy retrieval is keyword-oriented and not a semantic vector search yet.
- No database is used for persistence.
- No authentication, authorization, or reviewer queue is implemented.
- Receipt OCR and document parsing are out of scope for this prototype.
- Currency conversion, per-city lodging limits, and date-window validation are not implemented.
